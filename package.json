{
  "name": "haqms-workspace",
  "version": "1.0.0",
  "description": "Hospital Appointment and Queue Management System (HAQMS) - Full-Stack Healthcare Platform",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "db:setup": "npm run db:setup --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "npx concurrently \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
